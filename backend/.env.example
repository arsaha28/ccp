# Backend Environment Configuration
# Copy this file to .env and fill in your values

# Server Configuration
PORT=3001
NODE_ENV=development

# Frontend URL for CORS
FRONTEND_URL=http://localhost:5173

# Google Cloud / Dialogflow CX Configuration
# Your GCP Project ID where Dialogflow CX agent is deployed
DIALOGFLOW_PROJECT_ID=your-gcp-project-id

# Dialogflow CX Agent ID (UUID format)
# Find this in Dialogflow CX Console: Agent Settings > General > Agent ID
DIALOGFLOW_AGENT_ID=your-agent-uuid

# Dialogflow CX Location (e.g., us-central1, global, europe-west1)
# This should match where your agent is deployed
DIALOGFLOW_LOCATION=us-central1

# Path to your Google Cloud service account key JSON file
# Download from GCP Console: IAM & Admin > Service Accounts > Create Key
# The service account needs "Dialogflow API Client" role
GOOGLE_APPLICATION_CREDENTIALS=/path/to/your-service-account-key.json

# Optional: Dialogflow Language Code (default: en)
DIALOGFLOW_LANGUAGE_CODE=en
